{
  "name": "pearson-designs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "lint": "echo \"Add ESLint configuration and run it\"",
    "format": "echo \"Add Prettier configuration and run it\""
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "cssnano": "^5.0.0",
    "postcss": "^8.0.0",
    "vite": "^7.3.1"
  }
}
